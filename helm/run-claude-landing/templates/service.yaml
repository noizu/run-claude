apiVersion: v1
kind: Service
metadata:
  name: {{ include "run-claude-landing.fullname" . }}
  labels:
    {{- include "run-claude-landing.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    protocol: TCP
  selector:
    {{- include "run-claude-landing.selectorLabels" . | nindent 4 }}
